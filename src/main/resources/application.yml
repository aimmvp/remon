server:
  port: 8081

logging:  
  file:
    name: logs/app.log
    max-size: 10MB
    max-history: 5

management:
  endpoints:
    web:
      exposure:
        include: "refresh"

remon:
  urls:
    - "http://httpbin.org:11/get"
  domains:
    "[httpbin.org:11]":
      - "18.211.129.101:80"
      - "18.211.129.101:81"